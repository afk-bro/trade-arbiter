/**
 * Public surface of @trade-arbiter/engine. Additions should be narrow and
 * intentional — the engine exposes wiring primitives, not policy. Policy
 * (rules, strategies, adapters) lives in other packages.
 */
export {};
